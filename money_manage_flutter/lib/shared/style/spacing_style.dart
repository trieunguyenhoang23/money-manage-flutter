import '../../export/ui_external.dart';

class SpacingStyle extends StatelessWidget {
  const SpacingStyle({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 0.025.sh,
    );
  }
}
