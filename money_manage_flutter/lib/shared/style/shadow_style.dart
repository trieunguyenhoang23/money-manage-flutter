import 'package:flutter/material.dart';

BoxShadow shadowStyle(Color color, Offset offset, double blurRadius) {
  return BoxShadow(
    color: color,
    offset: offset,
    blurRadius: blurRadius,
  );
}
